# OUI lighttpd port override — place at /etc/lighttpd/conf.d/50-oui-port.conf
# This makes OUI listen on 8080 to avoid conflict with LuCI (uhttpd on port 80).
#
# Install path: files/lighttpd/50-oui-port.conf
# IPK installs to: /etc/lighttpd/conf.d/50-oui-port.conf

server.port = 8080
